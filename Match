var signature = this.getField("Signature utilisateur");
var signature2 = this.getField("Signature supérieur hiérarchique");

if (signature && signature2) {
    signature.display = display.hidden;
    signature2.display = display.hidden;
}
