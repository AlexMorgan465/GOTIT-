Sub CopierOcaocrDepuisTousCofidis()

    Dim WbSource As Workbook
    Dim WbDest As Workbook
    Dim WsSource As Worksheet
    Dim WsDest As Worksheet
    Dim Dossier As String
    Dim Fichier As String
    
    Application.ScreenUpdating = False
    Application.DisplayAlerts = False
    
    Set WbDest = ThisWorkbook
    Set WsDest = WbDest.Sheets("ocaocr")
    
    ' Chemin du dossier
    Dossier = "\\desktop\vba\"
    
    ' Cherche tous les fichiers cofidis.xlsm dans le dossier
    Fichier = Dir(Dossier & "*cofidis*.xlsm")
    
    Do While Fichier <> ""
        
        ' Ouvre le fichier
        Set WbSource = Workbooks.Open(Dossier & Fichier)
        Set WsSource = WbSource.Sheets("ocaocr")
        
        ' (Optionnel) vider avant chaque copie → sinon commenter
        WsDest.Cells.Clear
        
        ' Copie identique (aucun changement)
        WsSource.Cells.Copy Destination:=WsDest.Range("A1")
        
        ' Ferme le fichier source
        WbSource.Close SaveChanges:=False
        
        ' Fichier suivant
        Fichier = Dir
        
    Loop
    
    Application.DisplayAlerts = True
    Application.ScreenUpdating = True
    
    MsgBox "Copie terminée pour tous les fichiers !", vbInformation

End Sub
