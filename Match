Private Sub cmb_Supprimmer_Click()

    If vue.ListIndex < 0 Then
        MsgBox "Veuillez sélectionner une ligne à supprimer.", vbExclamation
        Exit Sub
    End If

    Dim ws As Worksheet
    Set ws = ThisWorkbook.Worksheets("temp")

    Dim selectedValue As String
    Dim i As Long, lastRow As Long

    ' Ici on prend une valeur unique (ex: dossier ou activité)
    selectedValue = vue.List(vue.ListIndex, 2) ' adapte si besoin

    lastRow = ws.Cells(ws.Rows.Count, 1).End(xlUp).Row

    ' Recherche de la ligne correspondante
    For i = 2 To lastRow
        If ws.Cells(i, 3).Value = selectedValue Then
            ws.Rows(i).Delete
            Exit For
        End If
    Next i

    MsgBox "Ligne supprimée avec succès.", vbInformation

    ' Rafraîchir la vue
    lastRow = ws.Cells(ws.Rows.Count, 1).End(xlUp).Row

    If lastRow >= 2 Then
        vue.List = ws.Range("A2:I" & lastRow).Value
    Else
        vue.Clear
    End If

End Sub
